**
1 input

 2   process
      create : insert(데이터베이스 명령)  read : select(데이터베이서 명령시)  update :update   delete : delete 업데이트와 딜리트는 동일  = crud

 3  db

 4 result 
 1,4 프론트엔드  .  2,3 백엔드..
 \

 동기식은 1~4순으로 데이터 진행됨
 비동기식은 순서가 없음
 예를들면 코드작성할때 타이머를 중간에 설정해서 실행시키면
 코든느 순서대로 읽었지만 타이머 때문에 순서가 뒤엉키게됨
 순서대로 흐르지 않고 나중에 타이머로 실행되는것들을 비동기식으로 본다.

ajax는 비동기식 방식이고 ?


dom(document object model)방식 **

-html 요소에 접근하느 ㄴ표준화된 방식
 - 기존 name 속성(웹 폼) 과 태그별 접근 방식에서 벗어나 
 오브젝트의 종류에 상관없이 id 속성을 사용하여 요소에 접근하는 방식

 *getElementByid('요소') 객체의 생성


